NODE_ENV=development
API_PORT=4000

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=worklink
DB_PASSWORD=worklink123
DB_NAME=worklink
DB_CONNECTION_LIMIT=10
DB_TIMEZONE=Z

REDIS_HOST=127.0.0.1
REDIS_PORT=6379

JWT_SECRET=worklink-local-development-secret
JWT_EXPIRES_IN=1d

# mock = no real gateway, always succeeds locally. Add a real provider
# key here once one is implemented (e.g. vnpay).
PAYMENT_GATEWAY_PROVIDER=mock
