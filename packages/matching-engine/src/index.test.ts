import test from 'node:test';import assert from 'node:assert/strict';import {matchWorkers} from './index.js';test('ranks matching worker first',()=>{const r=matchWorkers({requiredSkills:['event_reception'],district:'Quan 7',workers:[{id:'w1',fullName:'Lan',skills:['event_reception'],verifiedLevel:'V4',rating:4.8,completedJobs:10,serviceAreas:['Quan 7'],available:true}]});assert.equal(r[0]?.workerId,'w1')});
